# TODO

## 收藏 / 保存 / 历史（已实现，继续视觉打磨）

设计结论见 `CONTEXT.md`（History / 保存 / 收藏 词条）与 `docs/adr/0006-full-snapshot-history-and-self-contained-collection.md`。

三层模型：
- **历史**：自动记录全部查询，保留 50 条完整快照，会过期。
- **保存**：给整条历史记录打不过期标记（工具栏大按钮），是状态而非新条目。
- **收藏**：从面板精选片段进独立库，整块结构化卡 + 原句快照，自包含、有类型（短语/句型/例句/英文/文本）。

入口与交互已定：
- 片段级收藏入口在每个内容模块右上角，hover 出现。
- 收藏库主入口在 Hub 大窗口；浮层只做「已收藏 →查看」轻反馈。
- 点轻反馈跳到 Hub 收藏页并选中刚收的那条。
- 收藏卡「重新唤起 Lingobar」用自带快照重开；仅在换动作时才调 LLM。

### 已落地

- [x] `SavedPhrase` 扩字段：`type`、原句快照、完整结果快照。
- [x] `LingobarHubLibrary.collectionItems` 读取真实 `type`。
- [x] 片段级收藏入口：各内容模块右上角 hover 收藏按钮。
- [x] 工具栏大按钮语义改为「保存」（保存整条历史、不过期），与片段「收藏」分工。
- [x] 历史升级为完整结果快照，保留数 200 → 50，兼容旧的精简记录（ADR 0006）。
- [x] 浮层收藏成功后把新条目 id 传到 Hub，跳转并选中该卡。

### 后续打磨

- [ ] 片段级收藏 hover 动效与五种类型卡片可继续做视觉精修。
